import Vue from 'vue';

new Vue({
    el: '#skills-component',
    template: '#skills-list',
    data(){
        return {
            frontEndTitle: 'Front-end',
            workflowTitle: 'Workflow'
        }
    }
});